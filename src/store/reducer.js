
import avatar from './avatar.jpg';
import rentalLogo from './logo.png';

const initialState = {
    userAvatar:avatar,
    logo:rentalLogo
}

const reducer = (state = initialState,action)=>{
    return state;
}   

export default reducer;